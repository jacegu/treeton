require 'treeton/grammar/number'
require 'treeton/grammar/string'

module Treeton
  grammar Value
    include Number
    include String

    rule value
      string / number / true / false / null
    end

    rule true
      'true'
    end

    rule false
      'false'
    end

    rule null
      'null'
    end
  end
end
